- hosts:
    - all
  gather_facts: true
  roles:
    - base
    - intel-me-drivers
    - hyper-v
    - name: wsl
      when: ansible_os_installation_type == 'Client'
    - name: rancher-desktop
      when: ansible_os_installation_type == 'Client'
    - docker
    - msys2
    - k8s-client
    - azure-client
    - packer
    - terraform
    - vagrant
    - dotnet-sdk
    - go
    - rust
    - cue
    - dagger
    - visual-studio-code
    - visual-studio
